{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "StructuredTenderAnswer",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "summary", "status", "decisionStage", "sections", "gaps", "recommendation"],
  "properties": {
    "title": { "type": "string", "minLength": 1 },
    "summary": { "type": "string", "minLength": 1 },
    "status": {
      "type": "string",
      "enum": ["official", "simulated", "insufficient_evidence"]
    },
    "decisionStage": {
      "type": "string",
      "enum": ["opening", "document_review", "evaluation", "commission_recommendation", "award_decision", "unknown"]
    },
    "sections": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["heading", "body", "claimIds", "evidenceIds"],
        "properties": {
          "heading": { "type": "string", "minLength": 1 },
          "body": { "type": "string", "minLength": 1 },
          "claimIds": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 }
          },
          "evidenceIds": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "gaps": { "type": "array", "items": { "type": "string" } },
    "recommendation": { "type": "string", "minLength": 1 }
  }
}
