# TenderGraph Chat-First Demo Script

Final runtime: **2:41.600**. Narration and on-screen labels are in English.
The video contains no music or third-party brand footage. The public case uses
stable supplier aliases while preserving procedure scope, scores, evidence
anchors, hashes, and decision-stage boundaries.

The reproducible final render is
`public/tendergraph-build-week-chat-first-demo.mp4`. Generate the live capture
with `scripts/capture-live-codex-demo.mjs`, then render with
`scripts/render-submission-video.sh`. The renderer expects Python Piper 1.4 and
the `en_US-lessac-medium` voice through `PIPER_PYTHON` and `VOICE_MODEL`.

## 0:00-0:21 - Problem and product

**Screen:** Complete chat-first workbench and public procurement case.

**Narration:**

"Public procurement decisions are scattered across notices, evaluation tables,
supplier files, and later amendments. Teams still reconstruct who was
recommended, why others lost, and what changed. TenderGraph turns that
fragmented record into an auditable decision state, built with Codex and GPT
five point six."

## 0:21-0:41 - Evidence-bound answers

**Screen:** Evidence inspector with source, page, excerpt, parser, authority,
URL, and content hash.

**Narration:**

"Most AI products stop at summarization. Here, every answer is assembled from
promoted claims bound to exact evidence. The chat stays usable while the
inspector exposes source, page, quoted text, parser, authority, URL, and content
hash. A commission recommendation is never misrepresented as a signed
contract."

## 0:41-1:02 - Live Codex runtime

**Action:** Run the public question through the ChatGPT-authenticated Codex
surface. Show the running state, answer, trace, GPT-5.6 model, full Codex
Session ID, and 15/15 gates.

**Narration:**

"Now I run the question through a ChatGPT-authenticated Codex session, with no
API key. GPT five point six composes only from the admitted claim contract. The
trace retains the model and full session identifier, while fifteen code-owned
gates reject invention, evidence swaps, scope contamination, leakage, and
incomplete output."

## 1:02-1:27 - Ingestion and impact discovery

**Action:** Attach an official PDF, show eight extracted evidence anchors, then
analyze a later verified selection record and show one material corroboration,
6/6 gates, and human review.

**Narration:**

"Next I attach the official PDF. TenderGraph extracts eight hashed evidence
anchors without granting them authority. Codex then compares a later public
selection record with every active claim and finds one material corroboration.
Six independent gates validate scope, the complete claim partition, evidence
identity, action semantics, and shadow authority. Nothing changes until human
review."

## 1:27-1:44 - Corrective resolution

**Action:** Open the visibly synthetic Chile correction benchmark and run live
impact discovery.

**Narration:**

"The harder test is a later corrective resolution. TenderGraph identifies two
supersessions: the original winner and loss explanation are replaced, while the
award rule remains unchanged. The result matches the versioned reference
exactly and still cannot promote itself into official truth."

## 1:44-2:03 - Decision graph

**Screen:** Corrective diff, two shadow proposals, and unchanged rule.

**Narration:**

"This is not generic retrieval augmented generation. TenderGraph maintains a
decision graph across source versions, evidence dependencies, reviewed claims,
and explicit supersession. The same contracts already run across Chilean,
European, and United Kingdom tender structures and common procurement document
formats."

## 2:03-2:23 - Verified enforcement

**Screen:** Generated verification slide with fresh capture Session IDs.

**Narration:**

"The repository proves the enforcement layer: forty-four contract and
adversarial tests, twenty-three golden scenarios, fifteen composition gates,
six impact gates, and zero of eight injected faults admitted. The new capture
retains three fresh Codex session identifiers for composition and impact
discovery."

## 2:23-2:42 - Why this can win

**Screen:** Judging-criteria alignment and dated Build Week Git history.

**Narration:**

"Codex accelerated implementation, source verification, testing, and this
complete product redesign. The idea is deliberately different: not a chatbot
that summarizes a tender, but an auditable procurement decision compiler that
shows what changed, why it changed, and which evidence earns trust."

## Compliance checklist

- Runtime is below three minutes.
- Audio explains what was built and how both Codex and GPT-5.6 were used.
- The captured runs retain three full Codex Session IDs.
- Public buyer and supplier display names are anonymized.
- The correction benchmark is visibly synthetic.
- No music, third-party logos, or copyrighted footage is used.
- The narration is English and the final video must be public on YouTube.
